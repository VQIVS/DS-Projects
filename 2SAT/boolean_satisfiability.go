package TwoSAT

import (
	"fmt"
)

func RunBooleanSatisfiability() {
	fmt.Println("Boolean Satisfiability (SAT) is the problem of determining if there exists an assignment to the variables of a Boolean formula that makes the formula true.")
	fmt.Println("2-SAT is a special case of SAT where each clause in the formula has exactly two literals.")
}
