package MST

import "fmt"

func RunCutProperty() {
	fmt.Println("Running Cut Property example")
	fmt.Println("The Cut Property states that for any cut in a graph, the minimum weight edge that crosses the cut is part of the MST.")
	// Example illustration of the Cut Property
}
