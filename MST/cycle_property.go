package MST

import "fmt"

func RunCycleProperty() {
	fmt.Println("Running Cycle Property example")
	fmt.Println("The Cycle Property states that for any cycle in the graph, the maximum weight edge in the cycle is not part of the MST.")
	// Example illustration of the Cycle Property
}
